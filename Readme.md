If you ever need to add a new field (say, a field M[x]), you just need to:

Step 1-> Add M::usage to NLSMAlgebra.wl.

Step 2-> Add a recToLisp rule for M in NLSMLisp.wl.

Step 3-> Add a drawing rule (color/shape) in NLSMVisuals.wl.
